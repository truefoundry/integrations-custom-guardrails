fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
python-dotenv>=1.0,<2.0

# Async HTTP client. The rails fan out one Argus call per content unit and await
# them concurrently, so a blocking client would serialise them onto the
# threadpool and blow the gateway's 10s budget on agentic requests.
httpx>=0.27,<1.0
