fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
python-dotenv>=1.0,<2.0

# CoreWeave Weave scorers SDK. The [scorers] extra pulls in transformers,
# torch (CPU build is fine), and other ML deps required by WeaveToxicityScorerV1.
# Tested against 0.52.41; pin loosely to keep up with minor releases.
weave[scorers]>=0.52,<1.0
