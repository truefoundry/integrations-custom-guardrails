# guardrails-ai's PyPI package is currently quarantined (status: pypi:project-status: quarantined).
# Pinning to a specific GitHub tag bypasses PyPI but keeps the install deterministic.
# Re-evaluate before each deploy: if PyPI restores the package, switch back to plain pip install.
guardrails-ai @ git+https://github.com/guardrails-ai/guardrails.git@v0.9.3

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
python-dotenv>=1.0,<2.0
